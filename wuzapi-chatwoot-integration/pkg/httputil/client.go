package httputil

import (
	// "github.com/go-resty/resty/v2"
	// "time"
)

// This package provides shared HTTP client utilities.
// For instance, a function to create a new Resty client with common configurations
// (e.g., default timeouts, retry mechanisms, common headers) could be defined here.

/*
Example of a shared client creation function (if needed later):

func NewDefaultRestyClient() *resty.Client {
	client := resty.New().
		SetTimeout(10 * time.Second).
		SetRetryCount(3).
		SetRetryWaitTime(5 * time.Second).
		SetRetryMaxWaitTime(20 * time.Second)
	// Add any other common configurations, like user-agent
	return client
}
*/
